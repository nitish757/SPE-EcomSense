# filebeat.yml
filebeat.inputs:
  - type: container
    paths:
      - /var/log/containers/*.log
    processors:
      - add_kubernetes_metadata:
          in_cluster: true
    #  custom app log file input

output.elasticsearch:
  hosts: ["https://8a9cec2200264a9ead646c6ac0535c3f.us-east-2.aws.elastic-cloud.com:443"]
  api_key: "{{ elastic_api_key }}"

logging.level: info